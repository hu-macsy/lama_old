
This directory contains just a CMake configuration file to build
all projects, i.e.:

  - common
  - logging
  - tracing
  - ...

mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=$HOME/local
make
make install


-----------------------------

-DMKL_Is64=True         // sometimes helpful
-DCXX_SUPPORTS_C11=0    // forces using Boost
