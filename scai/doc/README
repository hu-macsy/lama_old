*** README: LAMA User Guide ***

Installation:
	Requirements:
		python-sphinx
		python-pygments
	Optional:
		latex (pdflatex + recommended fonts)

Introduction:
	"index.rst"  contains the document structure
	"installation.rst" or "tutorial/environmentVariables.rst" contain the real documentation content

Build (local):
	# make html 

Build (publish):
	# make json
	# make latexpdf
	
Extra:
	eclipse rest editor: http://resteditor.sourceforge.net/eclipse/
