import sys, os

# -- General configuration -----------------------------------------------------

# Add any Sphinx extension module names here, as strings. They can be extensions
# coming with Sphinx (named 'sphinx.ext.*') or your custom ones.
extensions = ['sphinx.ext.mathjax', 'sphinx.ext.intersphinx']

intersphinx_mapping = {
    'scaicommon': ('${SCAI_COMMON_DOC_DIR}', None),
    'scailogging': ('${SCAI_LOGGING_DOC_DIR}', None),
    'scaitracing': ('${SCAI_TRACING_DOC_DIR}', None),
    'scaitasking': ('${SCAI_TASKING_DOC_DIR}', None),
    'scaihmemo': ('${SCAI_HMEMO_DOC_DIR}', None),
    'scaikregistry': ('${SCAI_KREGISTRY_DOC_DIR}', None),
    'scaiblaskernel': ('${SCAI_BLASKERNEL_DOC_DIR}', None),
    'scaidmemo': ('${SCAI_DMEMO_DOC_DIR}', None),
    'scailama': ('${SCAI_LAMA_DOC_DIR}', None)
}

# The suffix of source filenames.
source_suffix = '.rst'

# The master toctree document.
master_doc = 'index'

# General information about the project.
project = u'LAMA'
copyright = u'2009-${LAMA_COPYRIGHT_YEAR} Fraunhofer Institute for Algorithms and Scientific Computing SCAI for Fraunhofer-Gesellschaft'

# The version info for the project you're documenting, acts as replacement for
# |version| and |release|, also used in various other places throughout the
# built documents.
#
# The short X.Y version.
version = '0.1'
# The full version, including alpha/beta/rc tags.
release = '${LAMA_VERSION} (${LAMA_VERSION_NAME})'

# The name of the Pygments (syntax highlighting) style to use.
pygments_style = 'tango'

highlight_language = 'c++'

# -- Options for HTML output ---------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
html_theme = 'sphinxdoc'

# Output file base name for HTML help builder.
htmlhelp_basename = 'LAMAdoc'

# -- Options for manual page output --------------------------------------------

# One entry per manual page. List of tuples
# (source start file, name, description, authors, manual section).
man_pages = [
    ('index', 'lama', u'LAMA Documentation',
     [u'Fraunhofer Institute for Algorithms and Scientific Computing SCAI for Fraunhofer-Gesellschaft'], 1)
]
